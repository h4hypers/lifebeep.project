# LifeBeep Email Configuration
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit the .env file to GitHub (it's in .gitignore)

# ========================================
# SMTP EMAIL SETTINGS
# ========================================

# For Gmail (Recommended)
# Steps to get Gmail App Password:
# 1. Go to https://myaccount.google.com/
# 2. Click "Security" in the left menu
# 3. Enable "2-Step Verification" (if not already enabled)
# 4. Scroll down to "2-Step Verification" section
# 5. Click "App passwords" at the bottom
# 6. Select "Mail" and "Other (Custom name)"
# 7. Type "LifeBeep" as the name
# 8. Click "Generate"
# 9. Copy the 16-character password (format: xxxx xxxx xxxx xxxx)
# 10. Paste it in SMTP_PASS below (remove spaces)

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_16_char_app_password_here

# Email addresses
EMAIL_FROM=your_email@gmail.com
EMAIL_TO=receiver1@example.com,receiver2@example.com

# ========================================
# ALTERNATIVE EMAIL PROVIDERS
# ========================================

# For Outlook/Hotmail:
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@outlook.com
# SMTP_PASS=your_password

# For Yahoo Mail:
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@yahoo.com
# SMTP_PASS=your_app_password

# For Custom SMTP Server:
# SMTP_HOST=smtp.yourserver.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_username
# SMTP_PASS=your_password

# ========================================
# EMAIL NOTIFICATION SETTINGS
# ========================================

# Multiple recipients (comma-separated, no spaces)
# Example: EMAIL_TO=person1@gmail.com,person2@gmail.com,person3@gmail.com

# From address (usually same as SMTP_USER)
# Example: EMAIL_FROM=lifebeep@gmail.com

# ========================================
# TESTING EMAIL CONFIGURATION
# ========================================

# After setting up, test with:
# 1. Start server: npm start
# 2. Open browser: http://localhost:5000
# 3. Click Settings (⚙️) and send test notification
# OR
# 4. Use curl command:
#    curl -X POST http://localhost:5000/notify -H "Content-Type: application/json" -d "{\"level\":\"High\",\"temperature\":25,\"humidity\":60}"

# ========================================
# SECURITY NOTES
# ========================================

# ⚠️ IMPORTANT:
# - NEVER commit your .env file to GitHub
# - The .env file is listed in .gitignore
# - Use App Passwords (not regular passwords) for Gmail
# - Keep your SMTP credentials secure
# - When deploying to a server, set these as environment variables

# ========================================
# TROUBLESHOOTING
# ========================================

# Error: "Invalid login"
# → Check SMTP_USER and SMTP_PASS are correct
# → For Gmail, ensure you're using App Password, not regular password
# → Make sure 2-Factor Authentication is enabled for Gmail

# Error: "Connection timeout"
# → Check SMTP_HOST and SMTP_PORT
# → Verify firewall allows outgoing connections on port 587
# → Try SMTP_PORT=465 with SMTP_SECURE=true

# Error: "self signed certificate"
# → Add to createTransport options: tls: { rejectUnauthorized: false }
# → This is not recommended for production

# No emails received?
# → Check spam/junk folder
# → Verify EMAIL_TO addresses are correct
# → Check server console logs for errors
# → Test SMTP credentials with online SMTP tester

# ========================================
# EXAMPLE WORKING CONFIGURATION (Gmail)
# ========================================

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=lifebeeph4@gmail.com
# SMTP_PASS=abcd efgh ijkl mnop  (16 chars from Google App Password)
# EMAIL_FROM=lifebeeph4@gmail.com
# EMAIL_TO=doctor@hospital.com,family@example.com
